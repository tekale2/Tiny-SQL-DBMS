#ifndef _PARSER_H
#define _PARSER_H

#include <vector>
#include <string>

using namespace std;

// helper function to tokenize the the input query into tokens
vector<string> tokenize(string &query);

#endif